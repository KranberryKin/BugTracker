{
  "name": "bugtracker",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd BugTracker.client && npm i",
    "setup:server": "cd BugTracker && npm i",
    "start": "node BugTracker/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  },
  "dependencies": {
    "moment": "^2.29.1"
  }
}
